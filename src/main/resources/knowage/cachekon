两级缓存：
    一级缓存(本地缓存)：sqlsession级别的缓存，一级缓存是一直开启的：sqlsession级别的一个map
        与数据库同一次会话期间查询到的数据会放在本地缓存中
        以后如果要获取相同的数据，会直接从缓存中获取，不会从数据库中获取
      一级缓存失效的情况：即没有使用到一级缓存的情况，就是还会向数据库发送查询数据的sql
        1、 sqlsession不同，一级缓存失效
        2、 sqlsession相同，但是查询的条件不同
        3、 sqlsession相同，但是两次查询之间执行了增删改操作（这次的增删改操作，可能会改变当前的数据）
        4、 sqlsession相同，手动清除了一级缓存（即缓存清空）
    tips:
        1、 springboot + mybatis : 一级缓存默认不生效。原因：
            当同一个线程开启事务时同一个sql查询多次会走一级缓存，而不开启事务时，
            每一查询都是不同的sqlsession即缓存为“失效”状态---即符合一级缓存失效的第一种情况
         2、
    二级缓存(全局缓存): 基于namespace级别的缓存 ： 一个namespace对应一个二级缓存
        工作机理：
            1、 一个会话: 查询一条数据,只要这个当前会话没有关闭，这个数据就会放在当前会话的一级缓存中；
            2、 如果会话关闭：一级缓存中的数据会被保存到二级缓存中，新的会话查询信息，就可以参照二级缓存中的内容
            3、 sqlsession ====> userDao -----> user
                                 deptDao -----> dept
                    不同的namespace查出的数据会放在自己对应的缓存中(map中)
         使用步骤：
            1、